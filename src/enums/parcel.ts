export enum ParcelStatus {
  POSTED = 'posted',
  ACCEPTED = 'accepted',
  ONTHEWAY = 'on_the_way',
  REQUESTFORDELIVERY = 'request_for_delivery',
  DELIVERED = 'delivered',
}

export enum PaymentStatus {
  UNPAID = 'Unpaid',
  PAID = 'Paid',
}